# v2.3.6 更新公告

- 修复完整离线便携版使用已有旧数据目录时启动失败：保留账号、房间、媒体、聊天和设置，迁移时跳过旧实例锁。
- 历史端口 `0` 自动迁移为默认 `20311`；固定端口被占用时显示具体端口并停止启动，不再随机切换。
- 修复在线成员人数上限双击事件冒泡，不会再误打开“界面风格”设置窗口；权限校验保持不变。
- 同步更新 Windows、Android、Web、Pages、README、部署教程和发布资产说明。
