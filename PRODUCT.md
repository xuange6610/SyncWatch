# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 想和朋友、家人或小型团队远程同步观影的普通用户。
- 希望把服务和数据保存在自己设备上的房主或服务器管理员。
- 需要通过 Windows、Android、macOS 或浏览器加入房间的成员。
- GitHub 仓库文档与展示站优先照顾第一次接触 Node.js、Electron 和自托管服务的新手。

## Product Purpose

SyncWatch同步观影是一套开源、自托管的多人同步观影与实时协作系统。房主启动服务器并创建房间，成员通过客户端或浏览器加入；系统同步播放、暂停、进度、倍速等状态，并提供聊天、弹幕、语音、屏幕共享、媒体管理和服务器管理能力。

成功意味着新手能根据文档完成启动、连接和基础安全配置，成员能稳定加入同一房间，房主能掌握自己的媒体与运行数据。

## Positioning

产品将同步观影、跨端连接、本机数据目录和可选的临时公网访问放在同一套可自行部署的软件中；它不是由第三方代管媒体与账号的在线影视平台。

## Operating Context

- 房主在 Windows、Linux、macOS 或受支持的 Android 设备上启动服务。
- 局域网成员使用内网地址连接；跨网络成员可使用 Cloudflare Tunnel 或自有 HTTPS 反向代理。
- 服务器媒体、账号、设置和密钥保存在 `SyncWatch同步观影-Data/`，迁移前需要停止服务并完整备份。
- GitHub Pages 仅用于产品展示、截图、下载入口和新手导览；它不能运行 Node.js、WebSocket、文件上传或 cloudflared。

## Capabilities and Constraints

- 支持同步播放、媒体库、多房间、聊天、弹幕、语音、屏幕与网页共享、成员管理、媒体处理、数据备份及可配置的 AI 中转。
- 支持 Windows、Web、Android 与 macOS 构建；具体可下载平台和架构以 GitHub Releases 中实际存在的构建产物为准。
- 公网访问需要网络连接；临时公网地址由第三方 Cloudflare Tunnel 提供，不承诺固定域名或长期可用。
- 应用 UI、文档和发布标题使用完整名称 `SyncWatch同步观影`。
- GitHub 仓库 slug 保留为 `SyncWatch`，因为仓库地址和既有链接需要稳定。
- 已发布的包名、协议头、Java/JNI 类路径和旧数据目录名属于兼容标识，不作为面向用户的作者姓名展示，不在无迁移方案时修改。

## Brand Commitments

- 产品名称：`SyncWatch同步观影`。
- 作者署名：`xuan`。
- 联系方式：QQ `2590813506`，微信 `love_020804`。
- 使用现有影院界面、应用图标和真实功能截图作为品牌与产品证据。
- 说明文字应准确、克制、适合新手，不把静态页面称为可运行完整服务。

## Evidence on Hand

- 真实应用界面位于 `public/`。
- 桌面端、服务端和移动端入口分别位于仓库根目录、`server/` 与 `mobile/`。
- 已有功能截图位于 `docs/screenshots/`。
- 自动化行为和发布契约测试位于 `tests/`。
- 当前没有可公开验证的用户数、性能基准、客户评价或服务可用性数据，展示站不得编造这些内容。

## Product Principles

1. 先让新手完成一次可用连接，再解释高级部署选项。
2. 所有下载与功能声明必须对应真实存在且经过验证的文件或代码路径。
3. 静态展示、体验版和完整服务的能力边界必须清楚区分。
4. 默认保护账号、密钥、IP、聊天记录、媒体文件名和个人信息。
5. 兼容性优先于无意义的全量重命名。

## Accessibility & Inclusion

面向网页的文档和展示站应支持键盘操作、可见焦点、语义化标题、图片替代文字、减少动态效果偏好，以及桌面和手机宽度下的无重叠阅读。

## 功能状态（以当前源码为准）

### 已经实现

- **服务器与账号**：`server/index.js` 提供 Express HTTP 服务、登录/注册、会话、密码更新、设备会话和管理员策略；注册账号和普通密码默认不受业务字符类型或字符数限制，仅保留用户名 1024 UTF-8 字节、密码 4096 UTF-8 字节的防滥用硬上限，管理员可显式启用更窄的字符集和字符数范围。内置 `admin` 只有在本次会话刚通过账号密码认证且仍处于首次初始化时，才由服务端签发一次性能力直接设置新密码；成功后立即撤销，本机免密会话和普通周期改密仍校验当前密码。注册名额申请支持按数量提交和部分/全部撤回，内置 `admin` 可删除申请记录。管理 API 只返回密码状态元数据，不返回明文或哈希；安全重置会撤销现有会话。首次运行会创建本地数据目录，默认端口为 5000。账号密码正确但房间号留空时，前端会提示进入账号可用房间或临时房间，并允许暂停/关闭提醒。
- **房间与同步播放**：服务端维护房间、成员、房间密码/人数限制和房主权限；Socket.IO 广播播放、暂停、进度、共享倍速、批量队列和成员状态。快进/拖动使用独立 `seek` 权限，旧数据默认跟随播放控制权；房主可设置跳过片头片尾，并可向指定成员申请切换清晰度。房主确认后可把房间配置、队列、聊天和媒体复制给申请方，内置 `admin` 可二次确认后覆盖迁移到指定目标房间。前台控制通道每 4 秒顺序探测一次，连续 3 次高延迟/超时才降级，连续 2 次健康样本后恢复。
- **媒体与上传**：HTTP 上传、文件夹扫描、媒体索引、字幕/图片/安全文本支持、HTTP Range 播放；超过 32 MiB 的媒体把开放 Range 限制为每段 8 MiB，平衡高延迟续段频率和拖动后废弃响应。网络错误或连续 12 秒无进展时最多执行 5 次渐进恢复，离线等待不消耗次数，恢复用尽后可回退已有流畅版。纯文本可按连续滚动或页码同步阅读，权威状态包含文件、精确 UTF-16 `characterOffset`、归一化位置、页码和 revision。客户端可以因屏幕宽度产生不同视觉行首，但不得用本地换行改写权威锚点；晚加入及断线重连客户端按同一逻辑字符恢复，服务器可关闭文本上传。客户端默认使用原画，用户可手动切换自动或流畅版；FFprobe 分析，FFmpeg 默认在上传后生成缩略图和约 480P、1 Mbps 的 H.264/AAC 低带宽流畅版，丢失的缩略图可重建，管理员可关闭自动兼容转换。
- **实时协作**：公聊、私聊、弹幕、公告、语音消息、好友/申请、在线状态和操作日志。账户级简洁模式只保留基础观影界面与进出房/进度通知；“仅聊天”过滤公告和系统通报；弹幕颜色、字号按账号保存。全屏时只显示媒体、弹幕和用户主动打开的边看边聊，普通通知与弹窗延后到退出全屏。成员头像单击打开资料，桌面双击或触摸端快速双击打开大图预览。
- **管理与数据**：权限组、注册审批、登录限流解限申请、房间复制/迁移、邮件配置、备份/导入导出、回收站、网络诊断、服务器设置和数据目录锁；超级管理员可修改显式稳定键及自动发现的固定界面文字/按钮，并导入、导出或恢复默认，在线客户端会实时收到更新。账户列表不返回明文密码，只显示密码状态与最近修改时间。敏感设备定位状态只发送给内置 `admin` 的受信管理会话。
- **游客边界**：游客临时房间不授予房主权限，与普通房间成员使用同一权限组；注册为正式账号后才可创建正式房间或申请建房额度。同一真实来源 IP 只允许一个活动游客会话；服务器仅对可信代理读取转发链，并从右向左剥离可信节点，不接受普通直连客户端伪造的转发头。
- **桌面启动网络**：Windows 服务器默认监听 5000 端口并自动选择局域网物理网卡；“系统 → 服务器启动设置”可改为指定 IPv4 网卡，保存后自动重启，所选网卡失效时回退自动选择，同时始终保留本机回环管理入口。
- **管理员快捷入口**：服务器设备可分别启用本机免密管理和本机免密以 `admin` 进入所选房间；两者都要求回环同源与主机令牌，管理专用会话可显式退出。普通账号与公网访问者仍走原认证流程。
- **多端入口**：浏览器 UI、Electron 服务器窗口、独立 Electron 客户端、Node 独立服务端和 Android WebView/手机服务器源码均存在并有对应构建配置。
- **一致帮助入口**：Web 顶栏“关于”和 Electron “帮助”菜单都指向当前 GitHub 项目主页与 Wiki。纯 Node.js 服务端明确标记为无原生菜单的控制台形态，并通过 `--help`、`--open-browser`、控制台摘要和 `服务器运行信息.txt` 提供等价的私密管理入口。
- **响应式工作区**：手机宽度下用“观影 / 聊天 / 片库 / 成员”四个随页面自然滚动的触控入口切换主模块，片库与成员以全宽抽屉展示，观影工具在小屏上重排为两列。顶栏功能使用不改变播放器高度的覆盖式菜单；390px 起为三列，较窄屏幕为两列滚动。账号行展开紧凑双列子菜单，并把“退出登录，保留账号密码”和普通“退出登录”放在首行，Android 触控目标保持至少 48px。
- **公网访问**：桌面端和独立服务端包含 Cloudflare Tunnel 调用、网络诊断和自有 HTTPS/反向代理配置路径；桌面端会根据物理直连与系统网络预检选择连接顺序，并只发布已经回探验证的地址。普通客户端不显示局域网服务 IP，公网状态与分享只使用一个可信公网 Origin/根地址，没有可信公网地址时不回退 LAN IP。
- **网页 URL 协作**：服务器校验并持久化房间共享 URL、标题、revision 与操作者，晚加入/重连按权威状态恢复；客户端沙箱 iframe 各自加载目标页面。跨域、登录态、地区、Cookie、CSP/X-Frame-Options 和第三方页面脚本不由 SyncWatch 绕过，完全同画面/同交互使用屏幕或浏览器标签页实时共享。
- **服务器下载中心**：服务器顶栏显示活动局域网地址和开放状态，并提供检查更新、GitHub、Latest 与下载中心；管理员只能把受校验的客户端文件写入固定下载目标。
- **通知控制**：服务器可独立关闭位置状态通知与位置授权请求；关闭后不是只隐藏前端，而是由服务端停止相应广播和申请。

### 部分实现或受环境限制

- **Android**：APK 嵌入 Node.js Mobile、WebView、手机服务器和 MediaProjection 屏幕共享；真实设备上的后台策略、通知权限、网络和签名环境会影响结果，不能只凭源码断言所有厂商都一致。
- **Android Node 兼容**：服务端为可能缺少 `crypto.randomUUID` 和 `Intl` 的 Node.js Mobile 18 运行时提供 UUID v4 与本地时间格式回退；回归测试覆盖管理员、同房游客、成员退出和游客重新登录，签名 APK 已在 Android 15 x86_64 模拟器中真实启动内嵌服务并验证登录。
- **macOS**：仓库包含 x64/arm64 构建配置、脚本和 CI 工作流；当前工作站是 Windows，macOS 成品应以对应 runner 的实际 Release 资产为准。
- **临时公网地址**：Cloudflare Quick Tunnel 依赖出站网络、DNS、代理/VPN/TUN 和 Cloudflare 服务，地址不固定；网络受限时应使用固定 HTTPS 反向代理。
- **AI 工作台**：`server/ai-relay.js` 和前端配置支持兼容 Responses API/Chat Completions 的中转，但模型、密钥、额度和可用能力由用户提供的服务决定。
- **屏幕/系统音频共享**：浏览器、Electron、Android 有不同的权限和 API；系统级音频与浏览器支持范围取决于平台版本和用户授权。
- **发布架构**：v2.2.5 候选已按固定清单准备 Windows、Android、macOS、Node.js 与 cloudflared 资产；下载文件以 GitHub Release 中实际存在的文件为准，候选在发布前不得宣称可下载。
- **发布数量标准**：当前 Latest 仍为 v2.2.4（26 个维护者资产 + 2 个源码归档）；v2.2.5 必须通过 [Release 资产清单](docs/release/release-manifest.md)门禁后才能切换为正式版。
- **当前候选能力**：v2.2.5 候选包含全屏手势与弹幕、简洁模式、地址隐私、房间复制/迁移、登录解限、更新检查、清晰度申请、独立快进权限、片头片尾和批量队列。17 个应用包与 9 个官方上游文件的最终证据待本轮 Actions 生成并核对。

### 计划开发

仓库当前没有一份独立、经维护者确认的产品路线图，因此以下不宣称为已承诺功能。未来需求应先通过 Issue/PR 确认，再更新本节：

- 更完整的平台兼容矩阵和真实设备自动化测试。
- 固定域名 Tunnel/HTTPS 的向导化配置与可观测性。
- 多节点或共享存储部署；当前数据目录和房间状态按单实例设计。
- 更细粒度的媒体转码队列、断点续传和运维指标。
- 与当前 Release 资产同步的跨平台下载索引和签名校验展示。

## 当前限制

1. 单个 `SyncWatch同步观影-Data/` 只能由一个实例写入；不要让桌面端、独立 Node 服务和 Docker 同时使用同一目录。
2. GitHub Pages 是静态站，不能替代自托管服务器，也不能在页面中真实创建房间、上传文件或开启 Tunnel。
3. 公网访问、邮件、媒体转码和 AI 中转分别依赖外部网络、SMTP、FFmpeg/FFprobe 和用户配置，失败时应按故障排查文档处理。
4. 版本兼容由协议、数据迁移和 Android 签名共同决定；覆盖安装前必须备份完整数据目录，Android 不应使用不同签名的 APK 覆盖安装。
